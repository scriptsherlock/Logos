
  # frontend

  This is the Logos frontend. The original Figma-generated project was used as the visual baseline.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
